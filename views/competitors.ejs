<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Meta Ads Spy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --bg-primary: #0d1117;
            --bg-secondary: #161b22;
            --bg-tertiary: #21262d;
            --text-primary: #f0f6fc;
            --text-secondary: #8b949e;
            --accent-blue: #58a6ff;
            --accent-green: #3fb950;
            --accent-orange: #d29922;
            --accent-red: #f85149;
            --border-color: #30363d;
        }
        
        body {
            background-color: var(--bg-primary) !important;
            color: var(--text-primary) !important;
        }
        
        .navbar {
            background-color: var(--bg-secondary) !important;
            border-bottom: 1px solid var(--border-color);
        }
        
        .card {
            background-color: var(--bg-secondary) !important;
            border: 1px solid var(--border-color) !important;
            color: var(--text-primary) !important;
        }
        
        .competitor-card {
            transition: transform 0.2s;
            border-left: 4px solid var(--accent-green) !important;
            background-color: var(--bg-tertiary) !important;
        }
        .competitor-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(63, 185, 80, 0.2);
        }
        
        .search-form {
            background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
            border-radius: 15px;
            padding: 2rem;
            color: var(--text-primary);
            border: 1px solid var(--border-color);
        }
        
        .metric-card {
            background: linear-gradient(135deg, #1f2937 0%, #374151 100%);
            color: var(--text-primary);
            border-radius: 10px;
            border: 1px solid var(--border-color);
        }
        
        .form-control, .form-select {
            background-color: var(--bg-tertiary) !important;
            border: 1px solid var(--border-color) !important;
            color: var(--text-primary) !important;
        }
        
        .form-control:focus, .form-select:focus {
            background-color: var(--bg-tertiary) !important;
            border-color: var(--accent-blue) !important;
            box-shadow: 0 0 0 0.2rem rgba(88, 166, 255, 0.25) !important;
            color: var(--text-primary) !important;
        }
        
        .btn-light {
            background-color: var(--accent-blue) !important;
            border-color: var(--accent-blue) !important;
            color: white !important;
        }
        
        .btn-light:hover {
            background-color: #4493f8 !important;
            border-color: #4493f8 !important;
        }
        
        .btn-outline-primary {
            color: var(--accent-blue) !important;
            border-color: var(--accent-blue) !important;
        }
        
        .btn-outline-primary:hover {
            background-color: var(--accent-blue) !important;
            border-color: var(--accent-blue) !important;
            color: white !important;
        }
        
        .btn-outline-secondary {
            color: var(--text-secondary) !important;
            border-color: var(--text-secondary) !important;
        }
        
        .btn-outline-secondary:hover {
            background-color: var(--text-secondary) !important;
            border-color: var(--text-secondary) !important;
            color: white !important;
        }
        
        .alert-danger {
            background-color: rgba(248, 81, 73, 0.1) !important;
            border-color: var(--accent-red) !important;
            color: var(--accent-red) !important;
        }
        
        .text-muted {
            color: var(--text-secondary) !important;
        }
        
        .text-primary {
            color: var(--accent-blue) !important;
        }
        
        .text-success {
            color: var(--accent-green) !important;
        }
        
        .text-warning {
            color: var(--accent-orange) !important;
        }
        
        .text-info {
            color: var(--accent-blue) !important;
        }
        
        .bg-light {
            background-color: var(--bg-tertiary) !important;
            color: var(--text-primary) !important;
        }
        
        .bg-primary {
            background-color: var(--accent-blue) !important;
        }
        
        .bg-secondary {
            background-color: var(--text-secondary) !important;
        }
        
        .bg-success {
            background-color: var(--accent-green) !important;
        }
        
        .progress {
            background-color: var(--bg-tertiary) !important;
        }
        
        .progress-bar {
            background-color: var(--accent-green) !important;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-search-dollar me-2"></i>Meta Ads Spy
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/">
                    <i class="fas fa-search me-1"></i>Search
                </a>
                <a class="nav-link active" href="/competitors">
                    <i class="fas fa-users me-1"></i>Competitors
                </a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
            </div>
        <% } %>

        <!-- Search Form -->
        <div class="search-form mb-4">
            <h2 class="mb-3"><i class="fas fa-users me-2"></i>Competitor Analysis</h2>
            <form method="GET" action="/competitors">
                <div class="row align-items-end">
                    <div class="col-md-8 mb-3">
                        <label for="searchTerm" class="form-label">Search for competitors in your niche</label>
                        <input type="text" class="form-control form-control-lg" id="searchTerm" name="q" 
                               placeholder="e.g., fitness, marketing, ecommerce, fashion"
                               value="<%= searchTerm %>">
                    </div>
                    <div class="col-md-4 mb-3">
                        <button type="submit" class="btn btn-light btn-lg w-100">
                            <i class="fas fa-search me-2"></i>Analyze Competitors
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <% if (competitors && competitors.length > 0) { %>
            <!-- Summary Stats -->
            <div class="row mb-4">
                <div class="col-md-3 mb-3">
                    <div class="card metric-card text-center">
                        <div class="card-body">
                            <div class="h3"><%= competitors.length %></div>
                            <div>Total Competitors</div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card metric-card text-center">
                        <div class="card-body">
                            <div class="h3"><%= competitors.reduce((sum, c) => sum + c.totalAds, 0) %></div>
                            <div>Total Ads</div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card metric-card text-center">
                        <div class="card-body">
                            <div class="h3"><%= Math.round(competitors.reduce((sum, c) => sum + c.totalImpressions.average, 0)).toLocaleString() %></div>
                            <div>Total Impressions</div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card metric-card text-center">
                        <div class="card-body">
                            <div class="h3">$<%= Math.round(competitors.reduce((sum, c) => sum + c.totalSpend.average, 0)).toLocaleString() %></div>
                            <div>Total Spend</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Competitors List -->
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0"><i class="fas fa-chart-bar me-2"></i>Competitor Rankings (by Total Impressions)</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <% competitors.forEach((competitor, index) => { %>
                            <div class="col-lg-6 mb-4">
                                <div class="card competitor-card h-100">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-3">
                                            <div>
                                                <div class="d-flex align-items-center mb-1">
                                                    <span class="badge bg-primary me-2">#<%= index + 1 %></span>
                                                    <h6 class="mb-0"><%= competitor.pageName %></h6>
                                                </div>
                                                <small class="text-muted">Page ID: <%= competitor.pageId %></small>
                                            </div>
                                        </div>

                                        <!-- Key Metrics -->
                                        <div class="row mb-3">
                                            <div class="col-6">
                                                <div class="text-center p-2 bg-light rounded">
                                                    <div class="h5 mb-0 text-primary"><%= competitor.totalAds %></div>
                                                    <small>Total Ads</small>
                                                </div>
                                            </div>
                                            <div class="col-6">
                                                <div class="text-center p-2 bg-light rounded">
                                                    <div class="h5 mb-0 text-success"><%= competitor.activeAds %></div>
                                                    <small>Active Ads</small>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Performance Metrics -->
                                        <div class="mb-3">
                                            <div class="d-flex justify-content-between align-items-center mb-2">
                                                <span class="small"><i class="fas fa-eye text-info me-1"></i>Total Impressions</span>
                                                <strong><%= Math.round(competitor.totalImpressions.average).toLocaleString() %></strong>
                                            </div>
                                            <div class="d-flex justify-content-between align-items-center mb-2">
                                                <span class="small"><i class="fas fa-dollar-sign text-warning me-1"></i>Total Spend</span>
                                                <strong>$<%= Math.round(competitor.totalSpend.average).toLocaleString() %></strong>
                                            </div>
                                            <div class="d-flex justify-content-between align-items-center mb-2">
                                                <span class="small"><i class="fas fa-chart-line text-primary me-1"></i>Avg Impressions/Ad</span>
                                                <strong><%= Math.round(competitor.averageImpressionsPerAd).toLocaleString() %></strong>
                                            </div>
                                            <div class="d-flex justify-content-between align-items-center">
                                                <span class="small"><i class="fas fa-money-bill text-success me-1"></i>Avg Spend/Ad</span>
                                                <strong>$<%= Math.round(competitor.averageSpendPerAd) %></strong>
                                            </div>
                                        </div>

                                        <!-- Platforms -->
                                        <div class="mb-3">
                                            <small class="text-muted">Platforms:</small>
                                            <div class="mt-1">
                                                <% competitor.platforms.forEach(platform => { %>
                                                    <span class="badge bg-secondary me-1"><%= platform %></span>
                                                <% }); %>
                                            </div>
                                        </div>

                                        <!-- Performance Bar -->
                                        <div class="mb-2">
                                            <small class="text-muted">Performance vs. Top Competitor</small>
                                            <div class="progress" style="height: 6px;">
                                                <div class="progress-bar bg-success" 
                                                     style="width: <%= Math.min(100, (competitor.totalImpressions.average / competitors[0].totalImpressions.average) * 100) %>%">
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Action Buttons -->
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-primary flex-fill" 
                                                    onclick="searchCompetitorAds('<%= competitor.pageName %>')">
                                                <i class="fas fa-search me-1"></i>View Ads
                                            </button>
                                            <a href="https://facebook.com/<%= competitor.pageId %>" 
                                               target="_blank" class="btn btn-sm btn-outline-secondary">
                                                <i class="fab fa-facebook me-1"></i>Page
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        <% } else if (searchTerm) { %>
            <div class="text-center py-5">
                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                <h5>No competitors found for "<%= searchTerm %>"</h5>
                <p class="text-muted">Try a different search term or check if your access token has the required permissions.</p>
            </div>
        <% } else { %>
            <div class="text-center py-5">
                <i class="fas fa-search fa-3x text-muted mb-3"></i>
                <h5>Search for competitors in your niche</h5>
                <p class="text-muted">Enter keywords related to your industry to discover and analyze your competitors' ad strategies.</p>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function searchCompetitorAds(pageName) {
            const searchForm = document.createElement('form');
            searchForm.method = 'POST';
            searchForm.action = '/search';
            
            const searchInput = document.createElement('input');
            searchInput.type = 'hidden';
            searchInput.name = 'searchTerms';
            searchInput.value = pageName;
            
            searchForm.appendChild(searchInput);
            document.body.appendChild(searchForm);
            searchForm.submit();
        }
    </script>
</body>
</html>